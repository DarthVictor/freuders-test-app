export enum LoadingState {
    NOT_INITED = "NOT_INITED",
    LOADING = "LOADING",
    SUCCESS = "SUCCESS",
    ERROR = "ERROR",
}

export enum TabId {
    QUOTES = "quotes",
    CONVERTER = "converter",
    HISTORY = "history",
}
